select *
from walid

DROP TABLE WALID
SELECT
	EMPLOYEE.ID,
	EMPLOYEE.NAME 'FIRST NAME',
	EMPLOYEE.DOB 'DATE OF BIRTH',
	COUNTRY.COUNTRY_NAME  'COUNTRY'
FROM EMPLOYEE LEFT JOIN COUNTRY
ON EMPLOYEE.COUNTRY_ID = COUNTRY.COUNTRY_ID

CREATE TABLE COUNTRY(
COUNTRY_NAME NVARCHAR(20) NOT NULL,
COUNTRY_ID INT NOT NULL
)

INSERT INTO COUNTRY VALUES('JORDAN' , 2 )

DELETE 
FROM COUNTRY
WHERE COUNTRY_ID= 2